#include <Arduino.h>
#include "..\Filter\KalmanFilter.h"

class Microphone{
private:
    uint8_t pin;
    KalmanFilter 

public:
    Microphone(uint8_t pin, int memory){

    }

    void Update(){
        //get running average for dc center

    }
};
